export const metadata = {
  title: "Event Reel Service Tirupati | Crewshoot",
  description: "Instant event coverage for concerts, birthdays, and celebrations in Tirupati.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
